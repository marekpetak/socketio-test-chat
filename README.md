# socketio-test-chat
Simple chat app developed while learning socket.io

#### Clone
`git clone https://github.com/marekpetak/socketio-test-chat.git`

#### Install dependencies
`yarn install or npm install`

`npm install -g nodemon`

#### Run
`nodemon index.js`

#### Browse
`localhost:3000`
